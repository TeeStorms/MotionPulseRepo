{
  "name": "motion-pulse-firestore-tests",
  "version": "1.0.0",
  "description": "Firestore security rules unit tests for Motion.Pulse",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.1",
    "firebase": "^10.12.0",
    "jest": "^29.7.0"
  }
}
